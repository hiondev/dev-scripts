#!/bin/bash
if [ "$1" = "help" ]; then
    echo "Build a the app container"
    exit
fi

source $SCRIPT_PATH/common/dummy

docker compose --project-directory $ROOT_DIR \
-f $HIONDEV_DIR/docker-compose.yaml \
--profile local-dev build app-local \
--build-arg=GOOGLE_MAPS_API_KEY=$GOOGLE_MAPS_API_KEY